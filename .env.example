# CiteCat Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values in the .env file
# 3. Install dependencies: npm install
# 4. Set up the database:
#    - Make sure PostgreSQL is running
#    - Run migrations: npx prisma migrate dev
#    - Generate Prisma client: npx prisma generate
# 5. Start the development server: npm run dev
# 
# For MinIO setup, see MINIO_SETUP.md
# =============================================================================

# Database Configuration
# PostgreSQL database URL for Prisma
DATABASE_URL="postgresql://username:password@localhost:5432/citecat_db?schema=public"

# Google Gemini AI Configuration
# Get your API key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your_gemini_api_key_here"

# Pinecone Vector Database Configuration
# Get your API key from https://app.pinecone.io/
PINECONE_API_KEY="your_pinecone_api_key_here"
PINECONE_INDEX_NAME="your_pinecone_index_name"

# MinIO Object Storage Configuration
# For local development, use the default values below
MINIO_ENDPOINT="http://localhost:9000"
MINIO_REGION="us-east-1"
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_BUCKET="uploads"

# PDF Processing Service Configuration
# URL of the external PDF processing service
PDF_SERVICE_URL="http://localhost:4000"

# Next.js Configuration
# Environment mode (development, production, test)
NODE_ENV="development"
